# Gap A-B-C Plan — 2026-08-26

## Goal
Close three MUST/near-MUST gaps identified in audit vs `2026-08-19-erp-v0-design.md`:
- A: Journal line-grid sticky totals footer + keyboard nav (spec §4 + §9)
- B: Dashboard totals period-scoped (spec §4 dashboard)
- C: XLSX import support via shared parser (spec §2/§4 "CSV or XLSX")

## Non-goals (deferred)
D/E/F: import preview wizard, activity history UI, docs refresh — separate follow-up.

## Contract (frozen by PM)
```ts
// src/lib/validation/journal.ts (additive)
export function sumLineAmounts(lines: Array<{debit: string; credit: string}>): {
  totalDebit: string; totalCredit: string; difference: string;
};
// decimal.js + lib/money toDbString half-up, same as balances.ts

// NEW src/server/imports/parser.ts
export type ParsedSheet = { headers: string[]; rows: Array<Record<string,string>> };
export class ImportParseError extends Error {}
export async function parseTabular(fileName: string, data: ArrayBuffer): Promise<ParsedSheet>;
// .csv -> papaparse trimHeaders, .xlsx/.xls -> exceljs sheet 1; Date cells -> 'YYYY-MM-DD' UTC; formula/rich-text -> String; empty trailing rows dropped; unsupported ext -> ImportParseError
```

## File-ownership matrix
| Slice | Files |
|---|---|
| A | `src/components/journal/JournalForm.tsx`, `src/lib/validation/journal.ts`, `tests/unit/lib/journal-form.test.ts` (new) |
| B | `src/app/(app)/dashboard/page.tsx` |
| C | `src/server/imports/parser.ts` (new), `src/server/actions/import-actions.ts`, `src/components/imports/CsvUpload.tsx`, `src/components/imports/JournalUpload.tsx`, `tests/unit/imports/parser.test.ts` (new) |
No overlaps — parallel merge safe.

## Worker briefs (TDD red-green-refactor)
- **A**: Red unit test for sumLineAmounts, then sticky footer (Total Debit/Credit/Difference via formatPHP, Difference 0 => "Balanced" green) + Enter advances cell→next row, Shift+Enter back. Keep post disable logic tied to same sum.
- **B**: If open fiscal_period exists, scope entries query to `gte(entry_date,start) lte(end)`; cards keep counts/totals consistent; no new deps.
- **C**: Red parser unit test (csv, xlsx with Date+formula, bad ext), then parser impl, then wire both import actions through parseTabular, update uploads accept.

## Verification (PM gate)
Per slice: `git diff --stat` vs matrix, `npm run typecheck`, slice `vitest run`. Final: `typecheck && lint && vitest run && build` then push. Playwright deferred (existing journal.spec covers critical path).

## Execution mode
PM-Orchestrated Multi-Agent: PM (this doc + blackboard) dispatches 3 workers in parallel, gates merges A->B->C commits.

## Risks
- ExcelJS date cell UTC handling — contract fixes via UTC parts.
- Keyboard nav must not break existing form submit — scope to line grid inputs only.
